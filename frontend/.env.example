# PII Filter Configuration
# The Azure PII Container endpoint (no authentication required)
PII_CONTAINER_ENDPOINT=http://pii-ozguler.eastus.azurecontainer.io:5000

# Alternative: If using Azure Language Service (cloud), use these instead
# PII_ENDPOINT=https://your-language-service.cognitiveservices.azure.com
# PII_API_KEY=your-api-key
